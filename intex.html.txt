<!DOCTYPE html>
<html lang="el">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Νταιάνα ❤️</title>

<style>
body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: linear-gradient(135deg, #ff9a9e, #fad0c4);
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  text-align: center;
}

.container {
  background: white;
  padding: 30px;
  border-radius: 20px;
  box-shadow: 0 10px 30px rgba(0,0,0,0.2);
  position: relative;
  z-index: 2;
  max-width: 450px;
}

h1 {
  color: #e91e63;
}

button {
  padding: 10px 20px;
  margin: 10px;
  font-size: 18px;
  border: none;
  border-radius: 10px;
  cursor: pointer;
  transition: 0.3s;
  position: relative;
}

.yes-btn {
  background-color: #4caf50;
  color: white;
}

.no-btn {
  background-color: #f44336;
  color: white;
  position: absolute;
}

.hidden {
  display: none;
}

.heart {
  position: fixed;
  top: -10px;
  font-size: 22px;
  animation: fall 4s linear infinite;
}

@keyframes fall {
  to {
    transform: translateY(110vh);
  }
}
</style>
</head>

<body>

<div class="container">

  <div id="question-section">
    <h1>Νταιάνα 💖</h1>
    <h2>Θέλεις να γίνεις η Βαλεντίνα μου;</h2>

    <div style="position:relative; height:100px;">
      <button class="yes-btn" id="yesBtn">Ναι ❤️</button>
      <button class="no-btn" id="noBtn" style="left:120px;">Όχι</button>
    </div>
  </div>

  <div id="success-section" class="hidden">
    <h1>Η ΝΤΑΙΆΝΑ ΕΙΠΕ ΝΑΙ 😍</h1>
    <p>Σ’ αγαπάω περισσότερο απ’ όσο φαντάζεσαι ❤️</p>
  </div>

</div>

<audio id="music">
  <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
</audio>

<script>
const yesBtn = document.getElementById("yesBtn");
const noBtn = document.getElementById("noBtn");
const questionSection = document.getElementById("question-section");
const successSection = document.getElementById("success-section");
const music = document.getElementById("music");

const phrases = [
  "Όχι",
  "Σίγουρα;",
  "Νταιάνα 😢",
  "Σε παρακαλώ",
  "Θα κλάψω...",
  "Μην το πατήσεις 😭"
];

let noCount = 0;

["mouseover", "touchstart"].forEach(event => {
  noBtn.addEventListener(event, () => {
    const x = Math.random() * 200;
    const y = Math.random() * 60;
    noBtn.style.left = x + "px";
    noBtn.style.top = y + "px";

    if (noCount < phrases.length - 1) {
      noCount++;
      noBtn.textContent = phrases[noCount];
    }
  });
});

yesBtn.addEventListener("click", () => {
  questionSection.classList.add("hidden");
  successSection.classList.remove("hidden");
  music.volume = 0.5;
  music.play().catch(() => {});
  startHearts();
});

function startHearts() {
  setInterval(() => {
    const heart = document.createElement("div");
    heart.classList.add("heart");
    heart.innerHTML = Math.random() > 0.5 ? "❤️" : "💖";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.animationDuration = (Math.random() * 3 + 2) + "s";
    document.body.appendChild(heart);

    setTimeout(() => heart.remove(), 5
